{
  "name": "wheee-dashboard",
  "version": "1.3.1",
  "type": "module",
  "description": "WHEEE Control Center Dashboard",
  "scripts": {
    "start": "./start.sh dev",
    "dev": "./start.sh dev",
    "prod": "./start.sh prod",
    "build": "cd frontend && npm run build",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "validate": "node validate.js",
    "test": "node validate.js"
  }
}
